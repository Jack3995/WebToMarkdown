package com.jack3995.webtomarkdown

// Перечисление для способа наименования файла
enum class FileNameOption {
    ASK_EVERY_TIME,    // Спрашивать каждый раз
    DEFAULT_NAME,      // По умолчанию (текущее имя)
    PAGE_TITLE         // Имя страницы (заголовок сайта)
}
