<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Тёмная тема -->
    <style name="Theme.WebToMarkdown" parent="android:Theme.Material.NoActionBar">
        <!-- Primary colors -->
        <item name="android:colorPrimary">@color/dark_primary</item>
        <item name="android:colorPrimaryDark">@color/dark_primary</item>
        <item name="android:colorAccent">@color/dark_secondary</item>
        
        <!-- Background colors -->
        <item name="android:colorBackground">@color/dark_background</item>
        <item name="android:textColorPrimary">@color/dark_on_background</item>
        <item name="android:textColorSecondary">@color/dark_on_surface_variant</item>
        
        <!-- Status bar colors -->
        <item name="android:statusBarColor">@color/dark_primary</item>
        <item name="android:windowLightStatusBar">true</item>
        
    </style>
</resources>